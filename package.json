{
  "name": "portshare-desktop",
  "private": true,
  "scripts": {
    "dev": "npm --prefix electron run dev",
    "start": "npm --prefix electron run start",
    "make": "npm --prefix electron run make"
  }
}
